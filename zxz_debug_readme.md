# zxz_调试模式使用说明

## 功能概述
为了加快代码调试速度，添加了全方位的调试模式功能：
1. 只使用前10%的数据集
2. 大幅减少训练轮次
3. 优化批次大小和学习率
4. 减少计算开销

## 使用方法

### 1. 启用调试模式（推荐）
修改 `zxz_debug_config.py` 文件：
```python
IS_DEBUG_MODE = True   # 启用调试模式
```

### 2. 关闭调试模式（完整训练）
修改 `zxz_debug_config.py` 文件：
```python
IS_DEBUG_MODE = False  # 关闭调试模式
```

### 3. 自定义调试参数
可以在 `zxz_debug_config.py` 中修改：
```python
DEBUG_PARAMS = {
    'epochs': 5,              # 训练轮次：60 -> 5
    'batch_size_train': 2,    # 训练批次：1 -> 2  
    'batch_size_test': 2,     # 测试批次：1 -> 2
    'num_threads': 1,         # 数据加载进程：默认 -> 1
    'lr': 0.005,              # 学习率：0.001 -> 0.005
    'lr_drop': 3,             # 学习率衰减：30 -> 3
}
DEBUG_DATA_RATIO = 0.1        # 数据使用比例：100% -> 10%
```

## 优化效果对比

| 参数 | 默认值 | 调试值 | 加速效果 |
|------|--------|--------|----------|
| 数据量 | 100% | 10% | **10倍加速** |
| 训练轮次 | 60 | 5 | **12倍加速** |
| 批次大小 | 1 | 2 | 减少显存占用 |
| 数据进程 | 多进程 | 1 | 减少进程开销 |
| 学习率 | 0.001 | 0.005 | 快速收敛 |

**总体加速效果：约100倍！** 🚀

## 工作原理

### 数据集优化
- 在数据集加载时，自动裁剪每个模态的数据
- 只保留前10%的数据文件
- 所有阶段（train/val/test）都应用相同裁剪

### 训练参数优化  
- 自动调整训练轮次：60轮 -> 5轮
- 优化批次大小：减少显存占用
- 调大学习率：在少量轮次内快速看到效果
- 减少数据加载开销

### 调试信息输出
```
zxz_启用调试模式，正在应用优化参数...
zxz_参数调整: epochs = 60 -> 5
zxz_参数调整: batch_size_train = 1 -> 2
zxz_参数调整: lr = 0.001 -> 0.005
zxz_调试模式 - train 阶段 模态0: 原始数据1000张，调试数据100张
zxz_调试模式 - train 阶段 模态1: 原始数据1000张，调试数据100张
```

## 适用场景
- ✅ **代码逻辑验证**：快速验证训练流程
- ✅ **参数调试**：测试不同的超参数组合  
- ✅ **错误排查**：快速定位代码问题
- ✅ **环境测试**：验证硬件配置是否正常
- ❌ **模型性能评估**：需要关闭调试模式

## 注意事项
- 调试模式生成的模型性能仅供参考
- 正式训练前务必设置 `IS_DEBUG_MODE = False`
- 建议先用调试模式验证代码，再进行完整训练

## 文件修改说明
- `zxz_debug_config.py`: 调试配置文件（新增）
- `main.py`: 添加调试参数自动应用逻辑
- `datasets/crack_dataset.py`: 添加数据集裁剪功能
- 所有新增代码都有 `zxz_` 前缀标注