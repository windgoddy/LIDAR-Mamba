# zxz_调试模式使用说明

## 功能概述
为了加快代码调试速度，添加了调试模式功能，可以只使用前10%的数据集进行训练和测试。

## 使用方法

### 1. 启用调试模式
修改 `zxz_debug_config.py` 文件：
```python
IS_DEBUG_MODE = True   # 启用调试模式
DEBUG_DATA_RATIO = 0.1 # 使用10%的数据
```

### 2. 关闭调试模式（使用完整数据集）
修改 `zxz_debug_config.py` 文件：
```python
IS_DEBUG_MODE = False  # 关闭调试模式
```

### 3. 自定义数据比例
如果想使用其他比例的数据（比如20%），可以修改：
```python
IS_DEBUG_MODE = True
DEBUG_DATA_RATIO = 0.2  # 使用20%的数据
```

## 工作原理
- 在数据集加载时，会自动裁剪每个模态的数据
- 只保留前N%的数据文件
- 所有阶段（train/val/test）都会应用相同的裁剪比例
- 会在控制台输出调试信息，显示原始数据量和调试数据量

## 注意事项
- 调试模式下生成的模型性能可能不如完整数据集训练的模型
- 用于快速验证代码逻辑和调试错误
- 正式训练时记得关闭调试模式

## 文件修改说明
- `zxz_debug_config.py`: 新增的调试配置文件
- `datasets/crack_dataset.py`: 在现有文件中添加了调试功能代码
- 所有新增代码都有 `zxz_` 前缀标注